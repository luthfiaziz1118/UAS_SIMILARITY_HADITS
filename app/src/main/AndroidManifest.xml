<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.mluthfiaziz.haditssimilarityapps">

    <application
        android:name=".MyApplication"
        android:allowBackup="true"
        android:icon="@drawable/book"
        android:label="@string/app_name"
        android:roundIcon="@drawable/book"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <meta-data
            android:name="AA_DB_NAME"
            android:value="SimilarityHadits.db" />
        <meta-data
            android:name="AA_DB_VERSION"
            android:value="5" />
        <meta-data
            android:name="AA_DB_MODEL"
            android:value="com.mluthfiaziz.haditssimilarityapps.model.Hadits" />
        <meta-data
            android:name="AA_MODELS"
            android:value="com.mluthfiaziz.haditssimilarityapps.model.Hadits" />

        <activity android:name=".activity.MainActivity"></activity>
        <activity android:name=".activity.SimilarityActivity" />
        <activity android:name=".activity.InputHadits" />
        <activity android:name=".activity.SplahScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>